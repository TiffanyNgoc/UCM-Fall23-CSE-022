#include <iostream>
using namespace std;

int main() {
    // Data we want to represent: average daily temperatures for one week

    // Questions we want to answer:
    // What the hottest day of the week?
    // What the coldest day of the week?

    return 0;
}